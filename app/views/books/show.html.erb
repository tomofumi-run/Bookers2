<main>
 <div class="container px-5 px-sam-0">
  <div class="row">
   <!--user_info-->
   <%= render 'books/list', books: @books %>
   
   <!--new book-->
   <div class="col-md-8 offset-md-1">
    <h2>Book</h2>
     <table class="table">
      <tbody>
       <tr>
      　<%= attachment_image_tag @book.user, :profile_image, size:'100x100' %>
        <td><%=link_to @book.user.name, user_path(@user.id) %></td>
        <td><%=link_to @book.title, book_path(@book.id) %></td>
        <td><%= @book.body%></td>
        <td><%=link_to "Edit", edit_book_path,class:"btn btn-sm btn-success" %></td>
        <% if @book.user == current_user %>
        <td><%=link_to "Destroy", books_path, class:"btn btn-sm btn-danger" %></td>
        <% end %>
      </tr>
    </tbody>
  </table>
  </div>
  </div>
  </div>
 </main>